---
phase: 01-testing-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/date-utils.ts
  - app/(app)/index.tsx
  - app/(app)/planner.tsx
  - app/(app)/shopping.tsx
  - components/MonthCalendarGrid.tsx
  - components/WeekCalendarStrip.tsx
  - __tests__/lib/validation.test.ts
  - __tests__/lib/date-utils.test.ts
autonomous: true
requirements:
  - TEST-02
  - TEST-05

must_haves:
  truths:
    - "All `validateEmail`, `validatePassword`, `validatePasswordMatch`, `validateName` behaviors are covered with happy path and edge cases"
    - "`lib/date-utils.ts` exists and exports `formatDateKey`, `getWeekRange`, `formatWeekLabel`"
    - "All 5 files that duplicated date utils import from `@/lib/date-utils` instead of defining them locally"
    - "`npm test -- --testPathPattern='validation|date-utils'` passes with no failures"
    - "`npx tsc --noEmit` exits 0 (no broken imports after extraction)"
  artifacts:
    - path: "lib/date-utils.ts"
      provides: "Single source of truth for date utility functions"
      exports: ["formatDateKey", "getWeekRange", "formatWeekLabel"]
    - path: "__tests__/lib/validation.test.ts"
      provides: "Unit tests for all 4 validation functions"
      min_lines: 50
    - path: "__tests__/lib/date-utils.test.ts"
      provides: "Unit tests for all 3 date utility functions with timezone-safe patterns"
      min_lines: 40
  key_links:
    - from: "app/(app)/index.tsx"
      to: "lib/date-utils.ts"
      via: "import { formatDateKey } from '@/lib/date-utils'"
      pattern: "from '@/lib/date-utils'"
    - from: "app/(app)/shopping.tsx"
      to: "lib/date-utils.ts"
      via: "import { formatDateKey, getWeekRange, formatWeekLabel } from '@/lib/date-utils'"
      pattern: "from '@/lib/date-utils'"
    - from: "__tests__/lib/date-utils.test.ts"
      to: "lib/date-utils.ts"
      via: "import { formatDateKey, getWeekRange, formatWeekLabel } from '@/lib/date-utils'"
      pattern: "from '@/lib/date-utils'"
---

<objective>
Extract duplicated date utility functions into a shared module, then write thorough unit tests for both validation functions and the newly created date-utils module. This plan fulfills TEST-02 (validation tests) and TEST-05 (date-utils extraction + tests).

Purpose: The extraction is a prerequisite for testability (D-04/D-05 from CONTEXT.md). Writing tests for validation and date-utils in the same plan is natural — both are in Wave 2 after the harness is ready, and they share no files.
Output: lib/date-utils.ts (new), 5 updated import sites, __tests__/lib/validation.test.ts, __tests__/lib/date-utils.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-testing-foundation/01-CONTEXT.md
@.planning/phases/01-testing-foundation/01-RESEARCH.md
@.planning/phases/01-testing-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Extracted from lib/validation.ts — all 4 exported functions -->
```typescript
// lib/validation.ts — 4 exported pure functions

export function validateEmail(email: string): string | null
// Returns null for valid email, error string otherwise
// Trims whitespace before checking
// Error: 'Email is required' for empty/whitespace-only
// Error: 'Please enter a valid email address' for invalid format

export function validatePassword(password: string): string | null
// Returns null for valid password
// Error: 'Password is required' for empty/falsy
// Error: 'Password must be at least 8 characters' for < 8 chars

export function validatePasswordMatch(password: string, confirmPassword: string): string | null
// Returns null when both match
// Error: 'Please confirm your password' for empty confirmPassword
// Error: 'Passwords do not match' when passwords differ

export function validateName(name: string): string | null
// Name is optional — returns null for empty/whitespace
// Error: 'Name must be at least 2 characters' if provided but < 2 chars after trim
```

<!-- Exact implementation to use for lib/date-utils.ts — extracted verbatim from shopping.tsx -->
```typescript
// lib/date-utils.ts — these are the exact implementations from the 5 duplicate locations

export function formatDateKey(date: Date): string {
  const y = date.getFullYear();
  const m = String(date.getMonth() + 1).padStart(2, '0');
  const d = String(date.getDate()).padStart(2, '0');
  return `${y}-${m}-${d}`;
}

export function getWeekRange(baseDate: Date): { start: Date; end: Date } {
  const monday = new Date(baseDate);
  const day = monday.getDay();
  const diff = day === 0 ? -6 : 1 - day;
  monday.setDate(monday.getDate() + diff);
  monday.setHours(0, 0, 0, 0);
  const sunday = new Date(monday);
  sunday.setDate(monday.getDate() + 6);
  return { start: monday, end: sunday };
}

export function formatWeekLabel(start: Date, end: Date): string {
  const opts: Intl.DateTimeFormatOptions = { month: 'short', day: 'numeric' };
  return `${start.toLocaleDateString('en-US', opts)} – ${end.toLocaleDateString('en-US', opts)}`;
}
```

<!-- Which functions each file uses (per D-04 in CONTEXT.md) -->
Files and their usage:
- app/(app)/index.tsx       — formatDateKey only
- app/(app)/planner.tsx     — formatDateKey only
- app/(app)/shopping.tsx    — formatDateKey, formatWeekLabel, getWeekRange (all three)
- components/MonthCalendarGrid.tsx  — formatDateKey only
- components/WeekCalendarStrip.tsx  — formatDateKey only

IMPORTANT: In each file, find the LOCAL function definition (not imported) and DELETE it.
Replace with an import from '@/lib/date-utils'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/date-utils.ts and update 5 import sites</name>
  <files>
    lib/date-utils.ts,
    app/(app)/index.tsx,
    app/(app)/planner.tsx,
    app/(app)/shopping.tsx,
    components/MonthCalendarGrid.tsx,
    components/WeekCalendarStrip.tsx
  </files>
  <read_first>
    - /Users/rayray/Desktop/projects/homecook/app/(app)/index.tsx — find local formatDateKey definition to remove
    - /Users/rayray/Desktop/projects/homecook/app/(app)/planner.tsx — find local formatDateKey definition to remove
    - /Users/rayray/Desktop/projects/homecook/app/(app)/shopping.tsx — find all three local definitions to remove
    - /Users/rayray/Desktop/projects/homecook/components/MonthCalendarGrid.tsx — find local formatDateKey definition to remove
    - /Users/rayray/Desktop/projects/homecook/components/WeekCalendarStrip.tsx — find local formatDateKey definition to remove
  </read_first>
  <action>
STEP 1 — Create lib/date-utils.ts:

Create `/Users/rayray/Desktop/projects/homecook/lib/date-utils.ts` with:
```typescript
/**
 * Shared date utility functions.
 * Extracted from: app/(app)/index.tsx, app/(app)/planner.tsx,
 *   app/(app)/shopping.tsx, components/MonthCalendarGrid.tsx,
 *   components/WeekCalendarStrip.tsx
 *
 * All functions use LOCAL timezone methods (getFullYear/getMonth/getDate)
 * not UTC methods, so dates match what users see on their device.
 */

/**
 * Format a Date as a YYYY-MM-DD key for Supabase date columns.
 */
export function formatDateKey(date: Date): string {
  const y = date.getFullYear();
  const m = String(date.getMonth() + 1).padStart(2, '0');
  const d = String(date.getDate()).padStart(2, '0');
  return `${y}-${m}-${d}`;
}

/**
 * Get the Monday–Sunday range for the week containing baseDate.
 */
export function getWeekRange(baseDate: Date): { start: Date; end: Date } {
  const monday = new Date(baseDate);
  const day = monday.getDay();
  const diff = day === 0 ? -6 : 1 - day;
  monday.setDate(monday.getDate() + diff);
  monday.setHours(0, 0, 0, 0);

  const sunday = new Date(monday);
  sunday.setDate(monday.getDate() + 6);

  return { start: monday, end: sunday };
}

/**
 * Format a week range as a human-readable label, e.g. "Mar 24 – Mar 30".
 */
export function formatWeekLabel(start: Date, end: Date): string {
  const opts: Intl.DateTimeFormatOptions = { month: 'short', day: 'numeric' };
  return `${start.toLocaleDateString('en-US', opts)} – ${end.toLocaleDateString('en-US', opts)}`;
}
```

STEP 2 — Update each of the 5 files:

For each file, read it first to find the exact local function definition text, then:
1. DELETE the local function body (the entire `function formatDateKey(...) { ... }` block)
2. ADD the import at the top of the imports section: `import { formatDateKey } from '@/lib/date-utils';`
   - For app/(app)/shopping.tsx: import all three: `import { formatDateKey, formatWeekLabel, getWeekRange } from '@/lib/date-utils';`

Do NOT change any other logic in these files. The function implementations are identical — this is a pure extraction.

STEP 3 — TypeScript check:
```bash
cd /Users/rayray/Desktop/projects/homecook && npx tsc --noEmit
```
Must exit 0. If TypeScript reports errors, fix them (likely a missing import or a type mismatch in the extracted function signature).
  </action>
  <verify>
    <automated>cd /Users/rayray/Desktop/projects/homecook && npx tsc --noEmit && echo "PASS: TypeScript clean" || echo "FAIL: TypeScript errors"</automated>
  </verify>
  <done>
    - `lib/date-utils.ts` exports `formatDateKey`, `getWeekRange`, `formatWeekLabel`
    - All 5 files import from `@/lib/date-utils` (grep returns matches in each file)
    - No local `function formatDateKey` definitions remain in the 5 files
    - `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write validation unit tests</name>
  <files>__tests__/lib/validation.test.ts</files>
  <read_first>
    - /Users/rayray/Desktop/projects/homecook/lib/validation.ts — read the full implementation before writing tests
  </read_first>
  <behavior>
    validateEmail tests (happy path + edge cases):
    - '' → 'Email is required'
    - '   ' (whitespace only) → 'Email is required'
    - 'notanemail' (no @) → 'Please enter a valid email address'
    - 'user@domain' (no TLD) → 'Please enter a valid email address'
    - '@domain.com' (no local part) → 'Please enter a valid email address'
    - 'user@example.com' → null
    - '  user@example.com  ' (with whitespace) → null (trims before checking)
    - 'user+tag@example.co.uk' → null
    - 'user@sub.domain.com' → null

    validatePassword tests:
    - '' → 'Password is required'
    - 'short' (< 8 chars) → 'Password must be at least 8 characters'
    - 'exactly8' (exactly 8 chars) → null
    - 'longpassword123' → null
    - '1234567' (7 chars) → 'Password must be at least 8 characters'

    validatePasswordMatch tests:
    - ('pass', '') → 'Please confirm your password'
    - ('pass123', 'different') → 'Passwords do not match'
    - ('pass123', 'pass123') → null
    - ('', '') → 'Please confirm your password' (confirmPassword empty)

    validateName tests:
    - '' → null (name is optional)
    - '   ' (whitespace only) → null (trims to empty = optional)
    - 'A' (1 char) → 'Name must be at least 2 characters'
    - 'Al' (2 chars) → null
    - 'Alice' → null
    - ' A ' (1 char after trim) → 'Name must be at least 2 characters'
  </behavior>
  <action>
Create `/Users/rayray/Desktop/projects/homecook/__tests__/lib/validation.test.ts`:

```typescript
import {
  validateEmail,
  validatePassword,
  validatePasswordMatch,
  validateName,
} from '@/lib/validation';

describe('validateEmail', () => {
  it('returns null for a valid email', () => {
    expect(validateEmail('user@example.com')).toBeNull();
  });
  it('returns null for email with plus tag', () => {
    expect(validateEmail('user+tag@example.co.uk')).toBeNull();
  });
  it('returns null for subdomain email', () => {
    expect(validateEmail('user@sub.domain.com')).toBeNull();
  });
  it('returns null for email with surrounding whitespace (trims before check)', () => {
    expect(validateEmail('  user@example.com  ')).toBeNull();
  });
  it('returns error for empty string', () => {
    expect(validateEmail('')).toBe('Email is required');
  });
  it('returns error for whitespace-only string', () => {
    expect(validateEmail('   ')).toBe('Email is required');
  });
  it('returns error for missing @', () => {
    expect(validateEmail('notanemail')).toBe('Please enter a valid email address');
  });
  it('returns error for missing TLD', () => {
    expect(validateEmail('user@domain')).toBe('Please enter a valid email address');
  });
  it('returns error for missing local part', () => {
    expect(validateEmail('@domain.com')).toBe('Please enter a valid email address');
  });
});

describe('validatePassword', () => {
  it('returns null for a valid password (>= 8 chars)', () => {
    expect(validatePassword('exactly8')).toBeNull();
  });
  it('returns null for a long password', () => {
    expect(validatePassword('longpassword123')).toBeNull();
  });
  it('returns error for empty string', () => {
    expect(validatePassword('')).toBe('Password is required');
  });
  it('returns error for password shorter than 8 chars', () => {
    expect(validatePassword('1234567')).toBe('Password must be at least 8 characters');
  });
  it('returns error for 1-char password', () => {
    expect(validatePassword('x')).toBe('Password must be at least 8 characters');
  });
});

describe('validatePasswordMatch', () => {
  it('returns null when passwords match', () => {
    expect(validatePasswordMatch('pass123', 'pass123')).toBeNull();
  });
  it('returns error when confirmPassword is empty', () => {
    expect(validatePasswordMatch('pass', '')).toBe('Please confirm your password');
  });
  it('returns error when passwords do not match', () => {
    expect(validatePasswordMatch('pass123', 'different')).toBe('Passwords do not match');
  });
  it('returns error when confirmPassword is empty even if password is empty', () => {
    expect(validatePasswordMatch('', '')).toBe('Please confirm your password');
  });
  it('is case-sensitive (Pass123 vs pass123)', () => {
    expect(validatePasswordMatch('Pass123', 'pass123')).toBe('Passwords do not match');
  });
});

describe('validateName', () => {
  it('returns null for empty string (name is optional)', () => {
    expect(validateName('')).toBeNull();
  });
  it('returns null for whitespace-only string (optional)', () => {
    expect(validateName('   ')).toBeNull();
  });
  it('returns null for valid 2-char name', () => {
    expect(validateName('Al')).toBeNull();
  });
  it('returns null for normal name', () => {
    expect(validateName('Alice')).toBeNull();
  });
  it('returns error for 1-char name', () => {
    expect(validateName('A')).toBe('Name must be at least 2 characters');
  });
  it('returns error for 1-char name with surrounding spaces', () => {
    expect(validateName(' A ')).toBe('Name must be at least 2 characters');
  });
});
```

After creating the file, run the tests to verify they all pass:
```bash
cd /Users/rayray/Desktop/projects/homecook && npm test -- --testPathPattern="validation"
```
  </action>
  <verify>
    <automated>cd /Users/rayray/Desktop/projects/homecook && npm test -- --testPathPattern="validation" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `__tests__/lib/validation.test.ts` exists with at least 20 test cases
    - `npm test -- --testPathPattern="validation"` exits 0 with all tests passing
    - No skipped tests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write date-utils unit tests</name>
  <files>__tests__/lib/date-utils.test.ts</files>
  <read_first>
    - /Users/rayray/Desktop/projects/homecook/lib/date-utils.ts — read the extracted implementation (created in Task 1)
  </read_first>
  <behavior>
    formatDateKey tests:
    - new Date(2024, 0, 15) → '2024-01-15' (basic format)
    - new Date(2024, 2, 5) → '2024-03-05' (zero-pads month and day)
    - new Date(2024, 11, 31) → '2024-12-31' (December)
    - new Date(2000, 0, 1) → '2000-01-01' (year 2000)
    IMPORTANT: Always use `new Date(year, month, day)` constructor — NOT `new Date('YYYY-MM-DD')` — to avoid UTC offset issues in CI

    getWeekRange tests:
    - Wednesday Jan 17, 2024 → start=Mon Jan 15, end=Sun Jan 21
    - Monday Jan 15, 2024 → start=Mon Jan 15, end=Sun Jan 21 (already Monday)
    - Sunday Jan 21, 2024 → start=Mon Jan 15, end=Sun Jan 21 (Sunday goes back to Monday)
    - Saturday Jan 20, 2024 → start=Mon Jan 15, end=Sun Jan 21
    - start.getDay() === 1 (Monday) always
    - end date is always start + 6 days
    - end - start === 6 days

    formatWeekLabel tests:
    - start=Jan 15, end=Jan 21 → 'Jan 15 – Jan 21'
    - start=Mar 28, end=Apr 3 → 'Mar 28 – Apr 3' (cross-month)
    - result contains ' – ' (en-dash separator)
  </behavior>
  <action>
Create `/Users/rayray/Desktop/projects/homecook/__tests__/lib/date-utils.test.ts`:

```typescript
import { formatDateKey, getWeekRange, formatWeekLabel } from '@/lib/date-utils';

// ── formatDateKey ────────────────────────────────────────────────────────
// IMPORTANT: Always use new Date(year, month, day) — local-time constructor.
// Do NOT use new Date('YYYY-MM-DD') which parses as UTC midnight and
// would produce wrong results in UTC-offset environments (CI).

describe('formatDateKey', () => {
  it('formats a date as YYYY-MM-DD', () => {
    const date = new Date(2024, 0, 15); // Jan 15, 2024
    expect(formatDateKey(date)).toBe('2024-01-15');
  });

  it('zero-pads single-digit month', () => {
    const date = new Date(2024, 2, 15); // Mar 15
    expect(formatDateKey(date)).toBe('2024-03-15');
  });

  it('zero-pads single-digit day', () => {
    const date = new Date(2024, 0, 5); // Jan 5
    expect(formatDateKey(date)).toBe('2024-01-05');
  });

  it('zero-pads both month and day', () => {
    const date = new Date(2024, 2, 5); // Mar 5
    expect(formatDateKey(date)).toBe('2024-03-05');
  });

  it('handles December correctly', () => {
    const date = new Date(2024, 11, 31); // Dec 31
    expect(formatDateKey(date)).toBe('2024-12-31');
  });

  it('handles year 2000', () => {
    const date = new Date(2000, 0, 1); // Jan 1, 2000
    expect(formatDateKey(date)).toBe('2000-01-01');
  });

  it('returns a string with format YYYY-MM-DD (length 10)', () => {
    const date = new Date(2024, 5, 15); // Jun 15
    expect(formatDateKey(date)).toHaveLength(10);
  });
});

// ── getWeekRange ──────────────────────────────────────────────────────────

describe('getWeekRange', () => {
  it('returns Monday as start for a Wednesday input', () => {
    const wednesday = new Date(2024, 0, 17); // Wed Jan 17
    const { start } = getWeekRange(wednesday);
    expect(formatDateKey(start)).toBe('2024-01-15'); // Mon Jan 15
  });

  it('returns Sunday as end for a Wednesday input', () => {
    const wednesday = new Date(2024, 0, 17); // Wed Jan 17
    const { end } = getWeekRange(wednesday);
    expect(formatDateKey(end)).toBe('2024-01-21'); // Sun Jan 21
  });

  it('handles Monday input (start of week — returns same Monday)', () => {
    const monday = new Date(2024, 0, 15); // Mon Jan 15
    const { start } = getWeekRange(monday);
    expect(formatDateKey(start)).toBe('2024-01-15');
  });

  it('handles Sunday input (rolls back to previous Monday)', () => {
    const sunday = new Date(2024, 0, 21); // Sun Jan 21
    const { start } = getWeekRange(sunday);
    expect(formatDateKey(start)).toBe('2024-01-15'); // Mon Jan 15
  });

  it('handles Saturday input', () => {
    const saturday = new Date(2024, 0, 20); // Sat Jan 20
    const { start, end } = getWeekRange(saturday);
    expect(formatDateKey(start)).toBe('2024-01-15');
    expect(formatDateKey(end)).toBe('2024-01-21');
  });

  it('start is always a Monday (getDay() === 1)', () => {
    const thursday = new Date(2024, 2, 7); // Thu Mar 7
    const { start } = getWeekRange(thursday);
    expect(start.getDay()).toBe(1);
  });

  it('end is always a Sunday (getDay() === 0)', () => {
    const thursday = new Date(2024, 2, 7); // Thu Mar 7
    const { end } = getWeekRange(thursday);
    expect(end.getDay()).toBe(0);
  });

  it('end is exactly 6 days after start', () => {
    const wednesday = new Date(2024, 0, 17);
    const { start, end } = getWeekRange(wednesday);
    const diff = (end.getTime() - start.getTime()) / (1000 * 60 * 60 * 24);
    expect(diff).toBe(6);
  });

  it('does not mutate the input date', () => {
    const input = new Date(2024, 0, 17);
    const inputTime = input.getTime();
    getWeekRange(input);
    expect(input.getTime()).toBe(inputTime);
  });
});

// ── formatWeekLabel ───────────────────────────────────────────────────────

describe('formatWeekLabel', () => {
  it('formats a same-month week range', () => {
    const start = new Date(2024, 0, 15); // Jan 15
    const end = new Date(2024, 0, 21);   // Jan 21
    const label = formatWeekLabel(start, end);
    expect(label).toContain('Jan 15');
    expect(label).toContain('Jan 21');
  });

  it('contains the en-dash separator', () => {
    const start = new Date(2024, 0, 15);
    const end = new Date(2024, 0, 21);
    expect(formatWeekLabel(start, end)).toContain(' – ');
  });

  it('formats a cross-month week range', () => {
    const start = new Date(2024, 2, 28); // Mar 28
    const end = new Date(2024, 3, 3);    // Apr 3
    const label = formatWeekLabel(start, end);
    expect(label).toContain('Mar');
    expect(label).toContain('Apr');
  });

  it('returns a non-empty string', () => {
    const start = new Date(2024, 5, 3); // Jun 3
    const end = new Date(2024, 5, 9);   // Jun 9
    expect(formatWeekLabel(start, end).length).toBeGreaterThan(0);
  });
});
```

After creating the file, run tests:
```bash
cd /Users/rayray/Desktop/projects/homecook && npm test -- --testPathPattern="date-utils"
```
  </action>
  <verify>
    <automated>cd /Users/rayray/Desktop/projects/homecook && npm test -- --testPathPattern="date-utils" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `__tests__/lib/date-utils.test.ts` exists with at least 15 test cases
    - `npm test -- --testPathPattern="date-utils"` exits 0 with all tests passing
    - No test uses `new Date('YYYY-MM-DD')` string constructor (all use `new Date(year, month, day)`)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -r "from '@/lib/date-utils'" /Users/rayray/Desktop/projects/homecook/app /Users/rayray/Desktop/projects/homecook/components` — returns matches in all 5 files
2. `grep -rn "function formatDateKey" /Users/rayray/Desktop/projects/homecook/app /Users/rayray/Desktop/projects/homecook/components` — returns 0 matches (no local duplicates remain)
3. `cd /Users/rayray/Desktop/projects/homecook && npx tsc --noEmit` — exits 0
4. `cd /Users/rayray/Desktop/projects/homecook && npm test -- --testPathPattern="validation|date-utils"` — exits 0, all tests pass
</verification>

<success_criteria>
- `lib/date-utils.ts` exports all 3 functions (formatDateKey, getWeekRange, formatWeekLabel)
- All 5 duplicate sites import from '@/lib/date-utils', no local definitions remain
- `npx tsc --noEmit` exits 0
- `npm test -- --testPathPattern="validation"` passes (20+ tests, all green)
- `npm test -- --testPathPattern="date-utils"` passes (15+ tests, all green)
</success_criteria>

<acceptance_criteria>
- `grep 'from .@/lib/date-utils.' /Users/rayray/Desktop/projects/homecook/app/\(app\)/index.tsx` returns a match
- `grep 'from .@/lib/date-utils.' /Users/rayray/Desktop/projects/homecook/app/\(app\)/shopping.tsx` returns a match
- `grep 'from .@/lib/date-utils.' /Users/rayray/Desktop/projects/homecook/components/MonthCalendarGrid.tsx` returns a match
- `grep 'function formatDateKey' /Users/rayray/Desktop/projects/homecook/app/\(app\)/index.tsx` returns NO match (local definition removed)
- `grep 'exports.formatDateKey\|export function formatDateKey' /Users/rayray/Desktop/projects/homecook/lib/date-utils.ts` returns a match
- `npm test -- --testPathPattern="validation"` exits 0
- `npm test -- --testPathPattern="date-utils"` exits 0
</acceptance_criteria>

<output>
After completion, create `.planning/phases/01-testing-foundation/01-02-SUMMARY.md` following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
